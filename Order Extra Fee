<!doctype html>
<html>
<body>
<div id="wrapper">
    <div class="header">
        <div class="header-left">
            <div class="logo-left">
                <a class="logo" href="{{store url=""}}">
                <img
                        width="{{var businessInformation.getLogoWidth()}}"
                        height="{{var businessInformation.getLogoHeight()}}"
                        src="{{var logo_url}}"
                        alt="{{var logo_alt}}"
                        border="0"
                />
                </a>
            </div>
            <div class="business-information">
                <p class="business-name">{{var businessInformation.getCompany()}}</p>
                <p>{{trans "Address"}}: {{var businessInformation.getAddress()}}</p>
                <p>{{trans "Phone"}}: {{var businessInformation.getPhone()}}</p>
                <p>{{trans "Tax ID"}} {{var businessInformation.getVatNumber()}}</p>
                <p>{{trans "Contact"}}: {{var businessInformation.getContact()}}</p>
                <p>{{trans "Registered"}}: {{var businessInformation.getRegistered()}}</p>
            </div>
        </div>
        <div class="header-right">
            <div class="info">
                <h1 class="info-title">{{trans "ORDER"}}</h1>
                <p>{{trans "Order "}} <b>#{{var order.increment_id}}</b></p>
                <p>{{trans "Date : "}} <b>{{var pdfInvoiceCustom.formatDate($order.created_at)}}</b></p>
                <p>{{trans "Amount : "}}<b>{{var order.formatPrice($order.getGrandTotal())|raw }}</b></p>
                <!-- BARCODE: Sugest Type: C39/EAN128A -->
                <p class="mp-invoice-barcode">
                    <barcode code="{{var order.increment_id}}" type="C39"/>
                </p>
                <!-- QRCODE -->
                <!-- <p class="mp-invoice-barcode"><barcode code="{{var order.increment_id}}" type="QR" size="0.8" error="M" disableborder="1"  /></p> -->
                <!-- Sugest Type:-->
            </div>
        </div>
    </div>
    <div class="clr"></div>
    <div class="content">
        <div class="address">
            <div class="billing-address">
                <h3 class="mp-block-heading">{{trans "Billing Address"}}</h3>
                <p>{{var formattedBillingAddress|raw}}</p>
            </div>
            <div class="payment-method">
                <h3 class="mp-block-heading">{{trans "Payment Method"}}</h3>
                <table class="paymentTable">
                    <tbody>
                    <tr>
                        <td>
                          <br>{{var payment_html|raw}}</br>
                           {{if order.getHasBillingExtraFee()}}
                     			{{layout handle="mp_billing_extra_fee" order=$order area="frontend" item=$order}}
				  			{{/if}}
                      </td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="clr"></div>
        {{depend order.getIsNotVirtual()}}
        <div class="address">
            <div class="shipping-address">
                <h3 class="mp-block-heading">{{trans "Shipping Address"}}</h3>
                <p>{{var formattedShippingAddress|raw}}</p>
            </div>
            <div class="shipping-method">
                <h3 class="mp-block-heading">{{trans "Shipping Method"}}</h3>
                <p>{{var order.getShippingDescription()}}</p>
               {{if order.getHasShippingExtraFee()}}
                    {{layout handle="mp_shipping_extra_fee" order=$order area="frontend" item=$order}}
			{{/if}}
            </div>
        </div>
        {{/depend}}
        <div class="clr"></div>
       <div class="address">
            <div class="shipping-method">
				{{if order.getHasExtraFee() }}
					<h3 class="mp-block-heading">{{trans "Extra Fee"}}</h3>
					{{layout handle="mp_extra_fee" order=$order area="frontend" item=$order}}
            	{{/if}}
            </div>
        </div>
        <div class="clr"></div>
    </div>

    {{layout handle="pdfinvoice_order_items2" order=$order itemBarcode=1}}

    <footer>
        {{depend orderNote}}
        <div class="footer-left">
            <h3 class="mp-note">{{trans "Notes"}}:</h3>
            <span style="font-style: italic;">{{var orderNote|raw}}</span>
        </div>
        {{/depend}}
    </footer>
</div>
</body>
</html>
